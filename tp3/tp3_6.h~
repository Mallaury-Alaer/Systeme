#include <stdio.h>
#include "tp3.h"
#include "tp3_6.h"

char *mon_strchr(char *s, int c)
{
  int taille_s = mon_strlen(s);
  int i=0;
  while( *(s+i) != c && i<=taille_s )
    {
      if( *(s+i) == c)
	{
	  return *(s+i);
	}
      i++;
    }
  return NULL;
}
