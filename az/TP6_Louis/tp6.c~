#include <stdio.h>
#include "tp6.h"
#include "tp6_1.h"
#include "tp6_2.h"
#include "tp6_3.h"
#include <unistd.h>

#include <ctype.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdlib.h>

int main(int argc,char **argv)
{
  printf("%d\n",argc);
  
  int de = open(argv[1], O_RDONLY), vers=open(argv[2], O_RDWR | O_CREAT, 0666);
  //  entete_bmp entete;
  
  if(de==-1)
    perror(argv[1]);
  else if(vers==-1)
    perror(argv[2]);
  else
    {
      /*if(verifier_entete(&entete)==1){
	printf("Erreur : taille entete\n");
	close(de);
	close(vers);
	return 0;
	}*/
      copier_bmp( de,  vers);
    }
  close(de);
  close(vers);
  return 0;
}

